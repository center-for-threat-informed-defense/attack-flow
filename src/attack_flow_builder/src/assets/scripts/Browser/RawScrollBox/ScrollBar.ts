export type ScrollBar = {

    /**
     * The scrollbar's {@link HTMLElement}.
     */
    el: HTMLElement;

    /**
     * True if the scrollbar is shown, false otherwise.
     */
    shown: boolean;

};
