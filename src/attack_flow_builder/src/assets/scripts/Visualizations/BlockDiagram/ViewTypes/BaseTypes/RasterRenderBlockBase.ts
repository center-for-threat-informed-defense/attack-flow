// import { RenderBlockBase } from "./RenderBlockBase";

// export abstract class RasterRenderBlockBase extends RenderBlockBase<> {

//     /**
//      * Creates a new DiagramRasterElement.
//      * @param id
//      *  The element's id.
//      * @param attrs
//      *  The element's attributes.
//      */
//     constructor(id: number, attrs: number) {
//         super(id, attrs);
//         this._lastHash = el.getContentHash();  // TODO: Move this to raster node only
//     }

//     public renderTo(context: CanvasRenderingContext2D): void {
        
//     }

//     protected abstract renderRasterTo(context: CanvasRenderingContext2D): void;

//     protected abstract getContentHash(): string;



// }
