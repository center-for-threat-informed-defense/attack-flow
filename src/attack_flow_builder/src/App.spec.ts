// import { mount } from '@vue/test-utils'
import { describe, it, expect } from "vitest";
// import App from './App.vue'

describe("App", () => {
    it("renders properly", () => {
        expect(0).toBe(0);
    // Mount Component
    // const wrapper = mount(App)
    // Check if App contains text "Hello World"
    // expect(wrapper.text()).toContain('Hello World')
    });
});
